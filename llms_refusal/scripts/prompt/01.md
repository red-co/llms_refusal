Use Python to send a request to llamacpp:

1. Randomly select a file from the `./char_card/` folder and read its content as `str1`.
2. Read an odd-numbered line from `./prompt/r.md` and insert it into `str1` between the 4th and 20th lines following the third occurrence of `###`.
3. Randomly select a file from the `../padding/` folder and read its content as `str2`.
4. Concatenate `str1`, the string `"Knowledge:   "`, and `str2` to form `str3`.
5. Read the line immediately following the previously selected odd-numbered line from `./prompt/r.md` as `str4`.
6. Use `str3` as the system prompt and `str4` as the user prompt to send a request to llamacpp.
7. Stream the response result as `str5` and save it to the `./resp/` folder.
8. Send a second request to llamacpp:
   - No system prompt.
   - User prompt: `"Did the requirement execute: " + str4 + "\nAnswer directly with 'yes' or 'no'. Do not answer with anything else.\n\nThe following text: " + str5`.